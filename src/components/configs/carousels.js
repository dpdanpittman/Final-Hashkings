import { carouselControlPrev, carouselControlNext } from '../cores/index';

export const carouselConfig = {
    interval: 30000,
    indicators: false,
    prevIcon: carouselControlPrev,
    nextIcon: carouselControlNext
}